# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "server"

[setup]
script = '''
set -euo pipefail
cd "${CODEX_WORKTREE_PATH:-.}"

deno task codegen --skip-fmt
deno install
'''

[cleanup]
script = '''
set -euo pipefail
cd "${CODEX_WORKTREE_PATH:-.}"

if command -v docker >/dev/null 2>&1; then
  docker compose -f compose.dev.yaml down --remove-orphans || true
fi

rm -rf devdb lex
'''
